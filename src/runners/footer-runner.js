import React from "react";
import ReactDOM from "react-dom";
import Footer from "../../components/footer/index";

const rootElement = document.getElementById("foot");

ReactDOM.render(<Footer />, rootElement);

