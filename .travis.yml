language:
  - node_js

node_js:
  - '10' # This is the LTS version of Node at time of writing: https://docs.npmjs.com/try-the-latest-stable-version-of-node

cache:
  npm: true # this cashes ~/.npm folder on Travis
  directories:
    # we also need to cache folder with Cypress binary
    - ~/.cache

install:
  - npm install
ß
script:
  - npm test
  - npm run build
  #- $(npm bin)/cypress run
  # --record --key ecf76371-5b01-40ad-a4bf-3179175cefc8 -> Add the test results inside Cypress Dashboard

notifications:
  slack:
    rooms:
      - tna-digital:C2GnodnLsxhwwGvr0xAcajzc#code-builds
